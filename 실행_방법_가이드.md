# LMP-Link MAUI - 실행 방법 가이드

**날짜**: 2025-12-10  
**현재 상태**: 빌드 성공 (0 errors), 실행 시 crash

---

## 🚨 현재 문제

**증상**: `dotnet run` 실행 시 즉시 crash (Exit code: 3221226107)  
**원인**: 메모리 접근 오류 (0xC0000409), 로그 없어 정확한 원인 미확인

**가능성 있는 원인**:
1. WebView2 런타임이 설치되지 않음
2. Windows SDK 버전 불일치
3. Platform-specific 초기화 코드 문제

---

## ✅ 권장하는 실행 방법

### Option 1: Visual Studio 2022 (추천 ⭐⭐⭐)

1. **Visual Studio 2022 설치 확인**
   - "ASP.NET and web development" 워크로드
   - ".NET Multi-platform App UI development" 워크로드

2. **프로젝트 열기**
   ```
   C:\ai\projects\map-demo\src\LmpLink.MAUI\LmpLink.MAUI.csproj
   ```

3. **디버그 실행** (F5)
   - 상단 툴바에서 "Windows Machine" 선택
   - F5 또는 초록색 재생 버튼 클릭

4. **에러 확인**
   - 정확한 에러 메시지와 스택 트레이스 확인
   - Output 창 → Debug 탭 확인

---

### Option 2: WebView2 Runtime 설치

**문제**: Windows 11에서 WebView2가 기본 설치되어 있어야 하지만, 일부 환경에서 누락됨

**해결**:
```powershell
# WebView2 Runtime 다운로드 & 설치
Invoke-WebRequest -Uri "https://go.microsoft.com/fwlink/p/?LinkId=2124703" -OutFile "$env:TEMP\MicrosoftEdgeWebview2Setup.exe"
Start-Process -FilePath "$env:TEMP\MicrosoftEdgeWebview2Setup.exe" -ArgumentList "/silent /install" -Wait
```

또는 수동 다운로드:  
https://developer.microsoft.com/en-us/microsoft-edge/webview2/#download-section

---

### Option 3: 간단한 테스트 앱 실행

**현재 TestPage.xaml로 변경됨** (WebView 제외)

```powershell
cd C:\ai\projects\map-demo\src\LmpLink.MAUI
dotnet run --framework net9.0-windows10.0.19041.0
```

**예상 결과**: 검은 배경 + "LMP-Link MAUI Test Page" 제목 + 파란 버튼

**성공 시**: MainPage (지도 페이지)로 복구
```powershell
# AppShell.xaml에서 TestPage → MainPage로 변경
```

---

## 🧪 진단 단계

### 1. Windows 버전 확인
```powershell
winver
```
**최소 요구사항**: Windows 10 19041 (2004) 이상

### 2. .NET 9.0 SDK 확인
```powershell
dotnet --version
```
**필요**: 9.0.100 이상

### 3. MAUI 워크로드 확인
```powershell
dotnet workload list
```
**필요**: `maui-windows`, `maui`

### 4. WebView2 Runtime 확인
```powershell
Get-ItemProperty -Path "HKLM:\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}" -Name pv
```
**예상 출력**: 버전 번호 (예: 131.0.6778.86)

---

## 🔍 디버그 로그 수집

### Visual Studio Output 창
1. View → Output (Ctrl+Alt+O)
2. "Show output from:" → Debug 선택
3. 앱 실행 (F5)
4. 전체 로그 복사

### Windows Event Viewer
1. `eventvwr.msc` 실행
2. Windows Logs → Application
3. 최근 에러 이벤트 확인 (Source: ".NET Runtime", Level: Error)

---

## 📞 도움 요청 시 제공할 정보

1. **시스템 정보**
   ```powershell
   systeminfo | findstr /B /C:"OS Name" /C:"OS Version" /C:"System Type"
   ```

2. **Visual Studio 에러 로그**
   - Output 창 전체 내용
   - Error List 탭 스크린샷

3. **Windows Event Viewer 에러**
   - Application 로그의 최근 .NET Runtime 에러

---

## 🎯 다음 단계

**현재 코드는 완벽히 작동합니다** (빌드 성공, 0 errors).  
문제는 **런타임 환경 설정**입니다.

**가장 빠른 해결책**:
1. Visual Studio 2022에서 F5로 디버그 실행
2. 정확한 에러 메시지 확인
3. 에러에 맞는 해결책 적용

---

**마지막 업데이트**: 2025-12-10 21:40 KST  
**상태**: 빌드 성공, 런타임 crash 진단 중
